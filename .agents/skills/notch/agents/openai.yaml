interface:
  display_name: "Notch"
  short_description: "Repo-specific patterns and workflows for notch"
  default_prompt: "Use the notch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true