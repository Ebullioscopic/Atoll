# UI migration staging

These files and resources are history-preserved visual candidates from
CodeIsland `v1.0.31`. They are deliberately excluded from the `CodeIslandUI`
target in Phase 1.

Move a component out of this directory only after removing assumptions about
CodeIsland's standalone panel and adapting it to Atoll-owned geometry,
navigation, typography, activity arbitration, and sanitized view models.
